## Commands

#### docker compose up --build
#### docker exec -it signup_mysql_db mysql -u root -psecret signup_db -e "SELECT * FROM users;"
#### docker exec -it signup_mysql_db mysql -u root -psecret signup_db
